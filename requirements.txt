# Dependencias de Python para PhoneCam (instalar con: pip install -r requirements.txt)
opencv-python>=4.8
Pillow>=9.0
numpy>=1.24

# Cámara virtual (opcional: para que Discord/Zoom/Meet vean el teléfono como webcam).
# Requiere instalar Unity Capture (ver README) o OBS Studio.
pyvirtualcam>=0.10

# Licencias PRO (verificación offline con Ed25519).
cryptography>=41

# QR code para descargar la app Android.
qrcode[pil]>=7.0
